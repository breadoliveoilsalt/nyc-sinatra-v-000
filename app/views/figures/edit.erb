<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>

    <form class="" action="/figures/<%= @figure.id %>" method="post">
      <input id="hidden" type="hidden" name="_method" value="patch">

      <!-- The values for "id=" below are based simply on what the tests/Capybara required -->

      <h1>Edit a figure!</h1>

      <p></p>
      Name: <input type="text" name="figure[name]" value="<%= @figure.name %>" id="figure_name" >

      <p></p>
      New Landmark: <input type="text" name="landmark[name]" value="" id="new_landmark">

      <p></p>
        <% Landmark.all.each do |landmark| %>
          <input type="checkbox" name="figure[landmark_ids][]" value="<%= landmark.id %>" id="landmark_<%= landmark.id %>" <%= 'checked' if @figure.landmarks.include?(landmark) %> > <%=landmark.name %> </input>
        <% end %>

      <p></p>
      New Title: <input type="text" name="title[name]" value="" id="new_title">

      <p></p>
      <% Title.all.each do |title| %>
        <input type="checkbox" name="figure[title_ids][]" value="<%= title.id %>" id="title_<%= title.id %>" <%= 'checked' if @figure.titles.include?(title) %> > <%= title.name %> </input>
      <% end %>

      <p></p>
      <input type="submit" name="" value="Edit Figure">

    </form>

  </body>
</html>
