<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>

    <form class="" action="/landmarks/<%= @landmark.id %>" method="post">
      <input id="hidden" type="hidden" name="_method" value="patch">
      <!-- The values for "id=" below are based simply on what the tests/Capybara required -->

      <h1>Edit a Landmark!</h1>

      <p></p>
      Name: <input type="text" name="landmark[name]" value="<%= @landmark.name %>" id="name" >

      <p></p>
      Year Completed: <input type="text" name="landmark[year_completed]" value="<%= @landmark.year_completed %>" id="year_completed">
      <p></p>
      Figure: <input type="text" name="figure[name]" value="" id="new_figure">

      <p></p>
        <% Figure.all.each do |figure| %>
          <input type="radio" name="landmark[figure_id]" value="<%= figure.id %>"  id="figure_<%= figure.id %>" <%= 'checked' if @landmark.figure = figure %> > <%=figure.name %> </input>
        <% end %>

      <p></p>
      <input type="submit" name="" value="Edit Landmark">

    </form>



  </body>
</html>
